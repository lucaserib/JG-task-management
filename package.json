{
  "name": "task-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gestão de Tarefas Colaborativo - Jungle Gaming Challenge",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:cov": "turbo run test:cov",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^3.1.1",
    "turbo": "^1.11.3",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "@nestjs/terminus": "^11.0.0",
    "@radix-ui/react-alert-dialog": "^1.1.15",
    "nest-winston": "^1.10.2",
    "winston": "^3.18.3"
  }
}
